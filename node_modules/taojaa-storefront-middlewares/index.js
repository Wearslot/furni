const storeMiddleware = require("./src/store.middleware");
const themeMiddleware = require("./src/theme.middleware");
const trafficMiddleware = require("./src/traffic.middleware");

module.exports = {
    storeMiddleware,
    themeMiddleware,
    trafficMiddleware
}